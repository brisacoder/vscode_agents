[project]
name = "vscode-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Reinaldo Penno", email = "reinaldo.penno@sonatus.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
vscode-agents = "vscode_agents:main"

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"
